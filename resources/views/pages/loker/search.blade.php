@extends('layouts.app')

@section('content')
    {{-- @if ($data->count() > 0)
        <ul>
            @foreach ($data as $row)
                <li>{{ $row->title }}</li>
            @endforeach
        </ul>
    @else
        <p>No results found.</p>
    @endif --}}
@endsection
